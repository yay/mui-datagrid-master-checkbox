import React from "react";
import ReactDOM from "react-dom/client";
import BasicDetailPanels from "./Demo.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BasicDetailPanels />
  </React.StrictMode>
);
